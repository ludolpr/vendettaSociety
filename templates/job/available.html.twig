{% extends 'base.html.twig' %}

{% block title %}Job Index{% endblock %}

{% block body %}
<div class="bg-[#2424249f] m-0">

    <div class="text-center justify-center content-center">
        <h1 class="text-2xl font-bold mb-4 mt-5 text-white justify-center m-5">Liste des metiers</h1>
        <p class="text-xl font-bold mb-4 mt-5 text-white justify-center m-5">
            Bienvenue sur la page des emplois. Vous y trouverez la liste des emplois disponibles. N'hésitez pas à la
            parcourir et à trouver celle qui vous convient le mieux. Pour plus de détails, cliquez sur le lien
            "afficher" à côté de l'emploi. </p>
    </div>

    <div class="flex justify-center">
        <table class="min-w-full divide-y divide-gray-200 bg-[#242424] text-white mb-6 w-3/4">
            <thead class="bg-gray-50 text-black">
                <tr>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-black uppercase tracking-wider">
                        Id</th>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-black uppercase tracking-wider">
                        Name</th>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-black uppercase tracking-wider">
                        Description</th>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-black uppercase tracking-wider">
                        Society Type</th>
                    <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-black uppercase tracking-wider">
                        Actions</th>
                </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200 text-black">
                {% for job in jobs %}
                {% if job.available == 0 %}
                    <tr>
                        <h2>Légal</h2>
                        <td class="px-6 py-4 whitespace-nowrap">{{ job.id }}</td>
                        <td class="px-6 py-4 whitespace-nowrap">{{ job.name }}</td>
                        <td class="px-6 py-4 whitespace-nowrap">{{ job.description }}</td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                            <a href="{{ path('app_job_show', {'id': job.id}) }}" class="text-black hover:text-black">Afficher</a>
                            <a href="{{ path('app_job_edit', {'id': job.id}) }}" class="text-black hover:text-black ml-4">Modifier</a>
                        </td>
                    </tr>
                {% else %}
                <tr>
                    <h2>illégale</h2>
                    <td class="px-6 py-4 whitespace-nowrap">{{ job.id }}</td>
                    <td class="px-6 py-4 whitespace-nowrap">{{ job.name }}</td>
                    <td class="px-6 py-4 whitespace-nowrap">{{ job.description }}</td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                        <a href="{{ path('app_job_show', {'id': job.id}) }}" class="text-black hover:text-black">Afficher</a>
                        <a href="{{ path('app_job_edit', {'id': job.id}) }}" class="text-black hover:text-black ml-4">Modifier</a>
                    </td>
                </tr>
                {% endif %}
            {% else %}
                <tr>
                    <td colspan="5" class="px-6 py-4 whitespace-nowrap text-center text-black">Aucun métier trouvé</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

    <div class="text-center mt-6">
        <a href="{{ path('app_job_new') }}"
            class="bg-[#fee501] text-black px-4 py-2 rounded-md shadow-sm hover:bg-white focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-white">Create
            New</a>
    </div>

</div>
{% endblock %}