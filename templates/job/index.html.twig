<div class="text-center justify-center content-center">
    <h1 class="text-2xl font-bold mb-4 mt-5 text-white justify-center m-5">Liste des metiers</h1>
    <p class="text-xl font-bold mb-4 mt-5 text-white justify-center m-5">
        Bienvenue sur la page des grade. Vous y trouverez la liste des grades avec un legère descriptions </p>
</div>

<div class="flex justify-center  ">
    <table class="min-w-full divide-y divide-gray-200 bg-[#242424] text-white mb-6">
        <thead class="bg-gray-50 text-black">
            <tr>
    
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-black uppercase tracking-wider">
                    Nom
                    </th>
                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-black uppercase tracking-wider">
                    Description
                </th>
               
               

            </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200 text-black col-auto">
                <tr  class="bg-white">
                
                    {% for grade in job.grades %}
                        <td>
                            {{ grade.name }}
                        </td>
                    {% endfor %}
                </tr>

           
            
        </tbody>
    </table>
</div>
